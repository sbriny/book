---
title: "20260424"
---

### 4.1 初版

**实现内容** 基础 Dock 功能：应用启动、窗口聚焦、最小化恢复、多实例管理、运行指示器、多实例徽章、悬浮 tooltip。

**主要设计决策**

- `DockIcon` 不持有业务逻辑，所有计算在 `Dock` 层完成，props 向下传
- tooltip 渲染在 `dock-wrapper` 内部（此版本尚未发现混合模式问题）